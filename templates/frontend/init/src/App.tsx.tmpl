import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      <div>
        <h1>{{projectName}}</h1>
        <p>{{projectDescription}}</p>
      </div>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
        </button>
        <p>
          Edit <code>src/App.tsx</code> and save to test HMR
        </p>
      </div>
      <p className="read-the-docs">
        Click on the Vite and React logos to learn more
      </p>
      <div className="info">
        <h3>Backend API</h3>
        <p>
          <a href="http://localhost:{{backendPort}}/docs" target="_blank" rel="noreferrer">
            API Documentation (Swagger)
          </a>
        </p>
        <p>Backend running on port {{backendPort}}</p>
      </div>
    </>
  )
}

export default App
