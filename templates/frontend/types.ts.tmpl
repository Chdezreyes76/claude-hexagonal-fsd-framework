export interface {{Domain}} {
  id: number;
  nombre: string;
  descripcion?: string;
  activo: boolean;
  createdAt?: string;
  updatedAt?: string;
}

export interface Crear{{Domain}}Request {
  nombre: string;
  descripcion?: string;
}

export interface Actualizar{{Domain}}Request {
  nombre?: string;
  descripcion?: string;
  activo?: boolean;
}

export interface {{Domain}}Filters {
  activo?: boolean;
  search?: string;
}
